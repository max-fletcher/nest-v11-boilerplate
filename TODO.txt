# File upload(DONE)
# Route not found response(DONE)
# Handle prisma constraint errors(e.g for unique constraint failure Prisma.PrismaClientKnownRequestError && error.code === 'P2002')(DONE)
# validation (Need to pivot to zod. Class validator doesn't enforce order.)(DONE)
# store encrypted/hashed password instead of raw password(DONE)
# user registration & login route with access and refresh tokens(DONE)
# find a way to use superrefine in registration and other validation schemas where necessary(DONE)
# Add rate limiter(DONE)
# Add cors(DONE)
# Add logger(DONE)
# Add custom exception handler(DONE)
# Relations(DONE)
# Nested Relations(DONE)
# Pagination for getAll calls(DONE)
# DB Transactions when disjointing relational data(DONE)
# Authorization and Access Control(DONE)
# PDF/Invoice generation
# Scheduled Tasks/ CRON job(DONE)
# Formatted responses(DONE)
# Redis(DONE)
# Queue (Bull + Redis)(e.g for email delivery) + Implement email service(??)
# Simplify exception pipeling(all-exception filter, main: app.useGlobalPipes(new ValidationPipe...), zodValidationPipe)(DONE)
# Add Swagger(DONE)
# Add health check endpoint
# Add request logging middleware


Possible path:  
# Learn nginx as reverse proxy + load balancer
# Learn CI/CD (GitHub Actions)
# Learn Kubernetes basics
# Learn microservices with NestJS
# Learn CQRS pattern
    ↓
  Months
    ↓
# Learn AWS/GCP basics (EC2, RDS, ElastiCache, S3)
# Learn monitoring (Prometheus + Grafana)
# Learn distributed tracing (OpenTelemetry)