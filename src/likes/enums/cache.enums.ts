// cache prefix — all user pagination keys start with this
export enum TLikeServiceCache {
  LIKE_QUERY_PAGINATION_CACHE_PREFIX = 'likes:qpagination:',
  LIKE_PAGINATION_CACHE_PREFIX = 'likes:pagination:',
  LIKE_SINGLE_CACHE_PREFIX = 'likes:'
}
