// cache prefix — all user pagination keys start with this
export enum TUserServiceCache {
  USER_QUERY_PAGINATION_CACHE_PREFIX = 'users:qpagination:',
  USER_PAGINATION_CACHE_PREFIX = 'users:pagination:',
  USER_SINGLE_CACHE_PREFIX = 'users:'
}
