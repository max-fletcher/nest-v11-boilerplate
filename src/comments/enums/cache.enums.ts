// cache prefix — all user pagination keys start with this
export enum TLikeServiceCache {
  COMMENT_QUERY_PAGINATION_CACHE_PREFIX = 'comments:qpagination:',
  COMMENT_PAGINATION_CACHE_PREFIX = 'comments:pagination:',
  COMMENT_SINGLE_CACHE_PREFIX = 'comments:'
}
